# translateI18n: One-Line I18n File Translation

`translateI18n` uses facebook/nllb-200-distilled-600M to translate one sentenced texts. 

### Installation and Run.

First, ensure you have the package installed:

```bash
installing locally:
# pip install .

pass the path and translate your files

```bash
translateI18n /Path/To/DirectoryToTranslate

In case you have other languages change the LANG_MAP
